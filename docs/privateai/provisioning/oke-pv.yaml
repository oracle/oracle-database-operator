apiVersion: v1
kind: PersistentVolume
metadata:
 name: oke-fsspv
 namespace: pai
spec:
 storageClassName: oci-fss
 capacity:
  storage: 100Gi
 accessModes:
  - ReadWriteMany
 mountOptions:
  - nosuid
 nfs:
  server: 10.0.XX.XX
  path: "/oml_models"
  readOnly: false
